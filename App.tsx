import React from 'react';

// NOTE: This file is a duplicate/leftover in the root directory.
// The actual application entry point is located in src/App.tsx.
// This stub exists to prevent TypeScript errors during the build process.

const App: React.FC = () => {
  return (
    <div style={{ padding: 20, color: 'red', textAlign: 'center' }}>
      <h1>Duplicate File Detected</h1>
      <p>You are viewing the root App.tsx. The application should be running from src/App.tsx.</p>
    </div>
  );
};

export default App;